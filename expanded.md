目前基础功能（地图，导航，定位已经完成），请你完成后续的拓展功能
- 用户登录与注册（先不用加邮箱验证）
- 添加增添自定义地点地点选项，用户可在地图上通过点击选择地点（默认“我的位置”），输入命名，然后这个地点将显示在用户的地图上，支持改名或删除。用户还可以选择发布该地点。输入对地点的描述（支持添加图片）发布，发布后，管理员账号会对用户发布的地点进行审核，审核通过后地点可作为一个其它用户也可看到的标注在地图上显示（会有一个特殊图标标识“这是其它用户添加的地点”）。注意，自定义地点，和其它用户发布的地点使用不同的颜色或图标进行区分。其它用户发布的地点和地图上原有的地点用一种标识。用户还可以对地图原本就有已有的地点或其它用户发布的地点进行改名和发布。如果地点与描述不符或名称有误，用户可进行反馈，由管理员进行处理。管理员账号可直接修改地图的地点名称，添加地点和描述，且一但修改可应用于全局（所有用户都可看到）。注意：普通用户可以自由进行账户内修改，但如果需要应用于全局（其它用户也生效）需要提交审核。
- 对于用户添加的地点，其它用户可以进行点赞或踩的操作（用图标不要用文字表示拉踩），用户可通过相关筛选条件选择展示哪些其它用户添加地点（如发布时间，赞的数量）。管理员添加的地点不受此限制，可直接显示。
- 用户可以选择隐藏/显示某些地点，应用仅在用户账户生效。
- 用户自定义地点，其它用户添加的地点，系统默认地点均可通过导航到达。
- 雨天水雷排查（特色功能）：由于西工大校园的一些地砖年久失修，雨天一些地砖在踩上时会翻砖，导致鞋变湿（俗称水雷）。用户可对水雷进行特殊标注和上传依旧需要审核）和账户删除或反馈全局删除（。雨天用户可选择打开“水雷排查功能”，系统定位用户的位置，当用户前进的方向直线5米内有水雷时会出现语音提示。用户也可在导航时选择“避开水雷的路线”，系统导航需要能够正确生成这样的路线。同样的逻辑可应用于“雪天易滑砖标注”。
- 危险地点标注：用户可自定义和上传危险地点，或者账户上删除或反馈全局删除失去实效性的危险地点。导航应该避开标注的危险地点。
- 远程服务器存储。所有应用相关数据存储在远程服务器上。应用启动时应该正确拉取服务器数据（用户数据和全局地图数据等）。相关修改也应当正确上传至服务器正确的数据库中。
- 所有后端功能应该使用ArkTs实现
- 服务器为华为云弹性服务器，IP为121.36.80.168，系统为Ubuntu